// Re-export useAuth from AuthProvider for backward compatibility
export { useAuth } from '@/components/providers/AuthProvider';
export type { AuthUser, AuthStep } from '@/components/providers/AuthProvider';
